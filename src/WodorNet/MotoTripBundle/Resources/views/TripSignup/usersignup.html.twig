{% extends 'WodorNetMotoTripBundle::mototrip.html.twig' %}

{% block content %}
<h1>TripSignup creation</h1>

<form action="{{ path('signup',{'tripId' :entity.trip.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('tripsignup') }}">
            Back to the list
        </a>
    </li>
</ul>

{% endblock %}
