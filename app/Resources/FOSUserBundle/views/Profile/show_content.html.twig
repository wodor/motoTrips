<div class="fos_user_user_show">
    <div><strong class="grid_4">{{ 'profile.show.username'|trans({}, 'FOSUserBundle') }}:</strong> {{ user.username }}</div>
    <div><strong class="grid_4">{{ 'profile.show.email'|trans({}, 'FOSUserBundle') }}:</strong> {{ user.email }}</div>
</div>
